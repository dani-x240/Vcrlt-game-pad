name: vcrlt_windows_receiver
description: "VCRLT Game Pad Windows Desktop Receiver (.exe) with ViGEmClient XInput Virtual Controller"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.2
  win32: ^5.2.0
  tray_manager: ^0.2.3
  window_manager: ^0.3.9
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
